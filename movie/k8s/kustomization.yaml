apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - proxy-deployment.yaml
  - proxy-service.yaml
  - movie-finder-deployment.yaml
  - orchestrator-deployment.yaml
  - orchestrator-service.yaml

configMapGenerator:
  - name: movie-config
    files:
      - ../config/agents.json
      - ../config/proxy-docket.json
